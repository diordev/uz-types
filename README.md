# uz-types

Rust uchun qayta ishlatiladigan type va value object'lar kutubxonasi.

## Features

- Strongly typed domain models
- Custom serialization/deserialization
- Built-in validation
- Backend loyihalar uchun reusable types

## Installation

`Cargo.toml`:

```toml
[dependencies]
uz-types = "0.2"